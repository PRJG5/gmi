<div class="container p-4">
	<div class="row justify-content-center">
		<div class="col-md-8">
			<div class="card">
				<div class="card-header blue white-text text-center">@yield('card-header')</div>
				<div class="card-body justify-content-center">@yield('card-body')</div>
			</div>
		</div>
	</div>
</div>

@yield('under-card')
@yield('script')
